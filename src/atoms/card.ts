// import { styled } from '../styles';

export const Card = (styled: any) =>
    styled('div', {
        display: 'flex',
        w: '100%',
        bg: 'white',
        rounded: '20px',
        justify: 'center',
        boxShadow: '0px 4px 10px rgba(0, 0, 0, 0.25)',
    });
